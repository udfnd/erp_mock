import { cssObj } from '../../styles';

export function NoSelectionPanel() {
  return (
    <>
      <div css={cssObj.panelHeader}>
        <h2 css={cssObj.panelTitle}>권한 설정</h2>
      </div>
    </>
  );
}
