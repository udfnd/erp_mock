import localFont from 'next/font/local';

export const pretendard = localFont({
  src: [{path: './PretendardVariable.woff2', style: 'normal'}],
  weight: '45 920',
  display: 'swap',
  variable: '--font-pretendard',
});
